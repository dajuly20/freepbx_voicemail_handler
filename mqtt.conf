# mqtt.conf - MQTT Broker Connection
# Installed to /etc/asterisk/mqtt.conf

# Broker address
MQTT_HOST="mqtt.mrz.ip"
MQTT_PORT=1883

# Authentication (leave empty if not required)
MQTT_USER=""
MQTT_PASS=""
